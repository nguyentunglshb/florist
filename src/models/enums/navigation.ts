export enum NavigationPath {
  HOME = "/",
  ABOUT_US = "/about-us",
  PLANTERS = "/planters",
  PLANTERS_DETAIL = "/planters/:_id",
  CONTACT = "/contact-us",

  FAVOURITE = "/favourite",
  PROFILE = "/profile",
  CART = "/cart",
  ORDER_TRACKING = "/order-tracking",
  CHECK_OUT = "/checkout",
  WISHLIST = "/wish-list",
  SEARCH = "/search",
  SEARCH_RESULT = "/search/:searchTerm",

  LOGIN = "/login",
}
